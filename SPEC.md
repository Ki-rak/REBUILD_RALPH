# RE:Build Agent 명세

상태: **v1.1 / UI06·AI 인증 분리 / 전체 제품 미완성**
기준: 2026-09-21, Asia/Seoul. 최신 사용자 지시와 실제 INPUT·양식을 우선한다.

## 완료 판정 원칙

제품 완료는 기능·데이터 정확성·반복 사용 가능성의 검증으로 판정한다. 배포 URL, 녹화, 발표자료, GitHub 게시와 행사 제출 준비는 별도 운영 작업이며 제품 완료를 대체하거나 제품 완료 판정에 섞지 않는다. ITB XLSX는 첫 구현 단계다. 그 성공이나 한 번의 시연만으로 프로그램 전체가 완성됐다고 선언하지 않는다. 확정된 기능을 구현·검증하고 후속 결과물·그래프의 진행 상태도 구별해 보고한다. 미구현 기능을 완료한 것으로 처리하지 않는다.

제품 핵심 검증은 AC01–16, AC23–42이다. AC17–22와 D1–D5/E1–E3는 별도 행사 작업이다. AC23 실제 AI 검증이 미완료면 AI 기능의 완료를 주장하지 않는다. 후속 기능은 구현된 범위만 완료로 표시한다.

## 문제와 완성품

입찰 검토 담당자는 과거 자료를 찾더라도 계약·현장 조건이 달라 그대로 재사용하기 어렵고, 판단의 근거를 다시 추적해야 한다. RE:Build Agent는 과거 프로젝트 자료를 지식화하고 신규 조건과 비교하여, 원문 근거가 연결된 업무 결과물을 생성하는 웹 서비스다. Windows에서 개발하고 심사위원이 사용할 수 있는 웹 데모로 배포한다. 최신 사용자 지시로 Vercel에 배포한다.

개발 기준선은 P01 과거 자료와 N01 신규 자료로 생성하는 ITB 분석표 XLSX다. **DB에 없던 신규 자료를 실제 업로드하여 기존 지식과 비교·연결하고 검토·승인 후 업무 결과물을 생성하는 기능**을 실제 입력 변경과 반복 사용으로 검증한다. 시연은 이 기능의 검증 사례이며 프로그램 완료 자체가 아니다. 사용자 검토·승인 후 파일을 다운로드하고 다시 열 수 있어야 한다. 화면은 제공된 DESIGN PREVIEW / 04의 프로젝트·지식 자료실·지식조회·근거 패널 구성을 따른다.

## 바운더리

- 가상 데이터만 사용한다. 실제 사내 시스템 연결, 실제 회사 자료, Docker, Local LLM, 복잡한 조직 역할·그룹 동기화, 결제는 P0에서 제외한다. 기본 로그인·사용자 소유권·자료 접근 제어는 포함한다. 심사위원이 접근해 실제 사용할 수 있는 데모 배포는 제출 필수 범위다.
- 최신 사용자 지시로 Vercel을 배포 대상으로 선택했다. DB도 사용자 지시에 따라 Supabase로 확정했다. Supabase PostgreSQL·Storage·Auth를 기본 구현안으로 삼는다. 계정 연결과 실제 검증은 아직 수행하지 않았다.
- 운영진이 LLM API 키를 배포할 예정이며 현재 수령 대기다. API 연결은 설정 존재만으로 사용 가능 처리하지 않는다. 허용된 제공처·계정·모델·비용 조건과 최소 실제 호출을 확인한다. 키는 서버에만 보관한다.
- API 미연결 시 검색·문서 추출·명시 규칙 비교·근거 확인·승인·파일 출력은 지원한다. 자유로운 의미 추론은 지원하지 않으며 AI Insight Mode에서 상태를 표시한다.
- 새 질문을 고정 답으로 대응하지 않는다. 문서·조건 변경이 비교와 결과에 반영되어야 한다.
- OCR·암호화·구형 Office·HWP는 P0 제외. `OCR_REQUIRED`/`UNSUPPORTED`/실패 사유를 표시하고 다른 파일 처리는 계속한다.

## P0 필수 흐름

1. **프로젝트·자료 구분:** 과거 폴더 대상과 파일 수를 표시하고 사용자가 승인한 파일만 등록한다. N01 신규 업로드는 원본을 별도 저장한 뒤 처리한다. N02와 섞지 않는다.
2. **변환:** DOCX·PPTX·XLSX·텍스트 PDF 및 MD/TXT/CSV/EML을 형식별 라이브러리로 읽어 MD+JSON에 저장한다. 표, 원래 값, 단위, 파일 hash, 문서 ID, locator를 유지한다.
3. **지식화·검색:** 프로젝트·문서·리스크/교훈·출처를 영속 DB와 객체 저장소에 보존한다. 로컬 SQLite 시험은 배포 영속성의 증명이 아니다. 동일 hash는 하나의 내용으로 관리하되 경로별 별칭을 남긴다. 단순 동의어/키워드 검색의 한계를 표시한다.
4. **비교·적용성:** 신규 계약 방식·통지기한·방류 허용량·염분/현장 조건을 과거와 비교한다. 판정은 행동 단위로 직접 적용/보정 후 적용/참고/적용 불가/검토 필요를 사용하며 이유·조건 차이·양쪽 근거를 표시한다.
5. **지식조회:** Search Mode는 실제 출처 우선 결과. AI Insight Mode는 연결된 모델/규칙 비교/미연결 상태를 구별한다. 실시간 모델을 사용하면 provider·model·호출/캐시 상태를 기록한다.
6. **근거 패널:** 주장마다 파일명, 개정/승인 상태, PDF page 또는 DOCX 문단/표·XLSX sheet/cell·PPTX slide/shape, 인용문과 원본 열기를 제공한다.
7. **초안·승인·출력:** 초안을 수정·반려·승인한다. 입력/초안 변경 시 승인 취소. 승인 없이는 최종 XLSX 생성 불가. 승인은 버전/hash에 결합한다.
8. **보존·재실행:** 원본·양식 불변, 중복 재수집 안전, 저장 후 재시작 복구, 오류 격리, 제품 이벤트 기록.

## 첫 입력 세트와 양식

INPUT 루트: `D:\REBUILD_RALPH\data\REBUILD_INPUT_v1\REBUILD_INPUT_v1`.

- 과거 P01: `01_PAST_PROJECTS/P01_Haeon_Metro_Package_2`의 DOCX, PDF, XLSX, PPTX, 현장 TXT를 우선 사용. 동일 DOCX 복사본은 중복 시험에만 추가한다.
- 신규 N01: `02_NEW_PROJECTS/N01_Haeon_Blue_Line_Package_4`의 DOCX, ITB Rev00 PDF, Rev01·Rev02 정정서 PDF, XLSX, 현장 TXT를 사용한다.
- P06 염수 경험은 첫 경로 성공 후 추가한다. 전체 15개 과거·2개 신규는 순차 확장하고 섞어서 평가하지 않는다.
- 출력: `03_OUTPUT_TEMPLATES/ITB_Analysis_Template.xlsx`, `ITB!A5:H5` 헤더, 6행부터 8개 항목 슬롯.
- 열: 항목 ID / ITB 조항 / 신규 조건 / 과거 사례 / 조건 차이 / 적용 판단 / 원문 근거 / 확인 필요 사항. 첫 경로에서는 최소 3개 항목을 검증하고, 근거 없는 칸은 검토 필요로 남긴다.
- 추천 이유: 비교·적용성·근거·미확정이 양식에 직접 있다. Risk Register는 별도의 발생가능성·영향·점수와 수식 재계산 검증이 필요하므로 다음 단계다.

## 데이터 계약

`SourceRef`: source_id, source_path, sha256, project_id, document_id, revision, approval_status, locator_type, locator, quote.

`SourceBlock`: source_ref, text, table/cells, original_value, unit, formula, cached_value, extraction_status. 없는 값은 null과 reason을 유지한다.

`Applicability`: historical_refs[], current_refs[], action, differences[], decision, rule_id 또는 provider/model, rationale, missing_information[], review_status.

`DraftApproval`: draft_id, revision, input_hashes[], evidence_hashes[], reviewer, approved_at, status. 승인 취소 이력은 보존한다.

원본 경로는 INPUT 기준 상대 경로를 보존한다. 서버 파일 열기는 등록된 source ID에 한정하고 임의 파일 경로를 받지 않는다.

## 원문에서 확인한 대표 판정

- N01 Rev01 승인 정정서 page 1: 통지기한 **14 calendar days**, 방류 허용량 **450 m3/day**. 변경하지 않은 계약·예산·JV·지체상금은 Rev00 유지.
- N01 Rev02 page 1은 FOR REVIEW. **7일·300 m3/day**를 유효 조건으로 채택하지 않는다.
- P01 DOCX paragraph 15: 담수 현장의 침전 방식만으로 염수 현장에 적용할 수 없음. 조사 절차 재사용과 펌프 수치 복사를 구별한다.
- N01 현장 TXT의 설계/관측 배수량은 확정값 없음. 방류 허용량을 설계 펌프 용량으로 변환해 확정하지 않는다.
- P01 DOCX paragraph 31의 28일 통지기한을 N01에 복사하지 않는다.

## 제품 기능 성공 기준

| ID | 합격 조건 | 검증 방법 |
|---|---|---|
| AC01 | 폴더 지식화 승인 전 변환/등록 없음 | 승인 전후 DB/파일 비교 |
| AC02 | 신규 원본 저장 후 변환, 원본 hash 일치 | 업로드 bytes와 저장본 hash |
| AC03 | 4개 주요 문서 형식의 본문·표·locator 유지 | P01 실제 파일에서 표적 문단/셀/slide/page 비교 |
| AC04 | 승인 Rev01 우선, 미변경 조항 유지 | Rev00→Rev01→Rev02 순차 입력 |
| AC05 | 신규 미확정/과거 관측/단위/계약 차이 노출 | N01 배수량, P01 담수 경험, 통지기한 사례 |
| AC06 | 모든 확정 주장에 해시 검증된 SourceRef | 인용문·locator 역조회, 잘못된 근거 차단 |
| AC07 | 3개 이상 ITB 항목이 근거·판정·검토사항 포함 | 양식 열과 출력 대조 |
| AC08 | 승인 없이 최종본 없음; 수정 시 재승인 필요 | 승인/반려/수정/입력 변경 UI 시험 |
| AC09 | 실제 XLSX 열림·한글/열/근거 정상 | 재읽기와 렌더/Office 표시 확인 |
| AC10 | 동일 파일 재등록이 지식/행을 중복 생성하지 않음 | P01 DOCX 별칭 재수집 |
| AC11 | 손상/스캔/미지원 입력 오류 격리 | 지원 파일과 혼합 업로드 |
| AC12 | 조건 변경이 결과를 바꾸고 근거 부족은 보류 | 파생 시험용 사본의 입력 변형 |
| AC13 | API 없는 경로 재현, AI 연결 상태 정확 | 외부 호출 차단 조건의 전체 흐름 |
| AC14 | EVALUATOR·preflight가 런타임에 유입되지 않음 | 입력 allowlist·구현 경계·파일 접근 시험 |
| AC15 | 재시작 후 전체 UI 경로 재현 | 새 파생 저장소에서 시작→출력 |
| AC16 | 원본 불변·실제 세션 로그/활동/실패 기록 보존 | 최초 hash 목록 대조·로그 export 재실행 |

깨진 근거·가짜 수치·원본 변경·열리지 않는 출력·승인 우회·정답 유입이 하나라도 있으면 P0 미완료다. 평가 96개 존재는 앱 합격을 의미하지 않는다.

## 확장 순서

P1: Risk Register → 원문과 연결된 관계 Graph → P06 및 전체 과거 자료 → N02/추가 조사 입력 → 프로젝트 비교표. P0 실패가 있으면 확장을 중단한다.

심의 PPTX 초안은 최신 사용자 지시에 따라 ITB/Risk 다음 구현 범위에 포함한다. P2: 계약 검토 DOCX, Lessons Learned, 사용자 지정 OnePager와 임의 양식, 시각 효과. 시간 부족 시 상태를 명시하고 제외한다.

## 실행 구조 제안

Windows 개발과 실제 웹 배포를 기준으로 기존 HTML/CSS/JavaScript 화면과 Python 3.12 FastAPI API를 우선 검토한다. 원본/파생 파일과 승인 상태는 함수 재시작에도 유지되는 외부 저장소를 사용한다. 로컬 SQLite 파일이나 함수 메모리를 배포본의 영속 저장소로 쓰지 않는다. DB는 사용자 확정 사항인 Supabase PostgreSQL을 사용하고, 파일은 Supabase Storage, 기본 인증은 Supabase Auth로 구현한다. 운영진 키를 비공개 서버 환경에 주입하고 실제 호출 후 LLM 연결 완료로 판정한다. 배포·LLM 계약 및 검증 순서는 docs/DEPLOYMENT.md와 docs/PROVIDER_AUTH.md를 따른다.

## 별도 행사 제출 체크리스트 — 제품 완료 판정에서 제외

`docs/SUBMISSION_REQUIREMENTS.md`의 D1–D5와 E1–E2는 필수, E3는 선택이다. 제품 기능으로서의 후순위 PPTX 생성과 별개로 행사용 발표자료는 필수다. 다음 기준을 모두 통과하기 전에는 최종 제출 준비 완료로 선언하지 않는다.

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC17 | 최종 결과와 확인 방법 제공 | README·시연 절차·실제 파일·제한 명시 |
| AC18 | 실제 작동하는 데모 링크 | 심사위원 접근 조건에서 DB 미등록 신규 파일 업로드부터 실제 결과물 다운로드까지 검증 |
| AC19 | Google Drive 데모 영상 링크 | 신규 파일 선택·업로드·분석·기존 근거 연결·승인·다운로드의 실제 동작 녹화와 재생 권한 확인 |
| AC20 | Public GitHub 저장소 | 비로그인 열람·실행 안내·공개 파일 목록 점검 |
| AC21 | PDF/PPT/PPTX 발표자료 최대 5페이지 | 페이지 수와 렌더/재개봉 확인 |
| AC22 | 실제 /goal 원문 + 주요 JSONL 1개 | 동일 세션에 실제 입력과 결과가 함께 기록됐는지 확인 |

제출 화면의 개별 필드를 실제 확인하며, 하나의 ZIP으로 필수 항목을 대체하지 않는다. 호스팅은 Vercel을 사용하며 실제 계정·프로젝트·저장소 연결 및 공개 접근 검증은 남아 있다.

## 배포·LLM 검증 보완

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC23 | 운영진 키로 실제 LLM 호출과 근거 검증 | provider/model·실제 응답/usage·유효 SourceRef 확인. 키 미수령/실패는 미통과로 표시하고 규칙 경로와 구별 |
| AC24 | 자료·승인·출력의 영속성 | 서버 재시작·새 세션 후 원본 hash·초안·승인 상태·파일 다운로드 재확인. Vercel 배포 검증은 별도 D2 작업 |

API 연결 대기는 제품 데이터 경로 개발을 막지 않지만 LLM 검증 PASS로 대체되지 않는다. 공개 데모는 Windows 로컬 폴더 경로에 의존하지 않는다. 큰 업로드의 지원 범위와 오류를 명확히 표시한다.

## Provider 설정·로그인·SSO — 사용자 추가 범위

설정 화면에서 Provider 유형·API 규격·base URL·model·인증 방식·비밀키 설정 여부·연결 시험 결과와 프로젝트별 활성 Provider를 관리한다. 운영진 API와 사내 LLM을 선택 가능한 유형으로 표시하되 확인된 규격만 실제 adapter로 지원한다. 키는 서버 환경 또는 접근이 제한된 비밀 저장소에 보관하며 브라우저 저장소와 일반 DB 열·로그에 넣지 않는다. 자세한 기능·상태 계약은 `docs/PROVIDER_AUTH.md`를 따른다.

기본 로그인/로그아웃/세션 복구·만료와 사용자 소유 자료 접근 제어를 구현한다. SSO는 설정 유형·callback 안내·미설정 상태를 보여준다. 사용자가 사내 공급자는 추후 결정한다고 했으므로 이번에는 실제 회사 IdP·사내 LLM 연결을 완료 조건으로 두지 않는다. 이 제외는 운영진 API의 실제 연결 검증 대기를 없애지 않는다.

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC25 | Provider 프로필 저장·선택·변경과 연결 상태 정확 | 미설정/오류/설정 변경 상태 전환; 실제 성공 없이 CONNECTED 금지; 키·헤더 비노출 |
| AC26 | 기본 로그인·로그아웃·세션 복구·만료 작동 | Supabase 연결 후 실제 세션 시험, 미설정은 미검증 표시 |
| AC27 | SSO 설정 화면과 미설정 안내 제공 | 실제 IdP 미연결 상태 명시, 가짜 로그인 성공 금지; 사내 실제 연결은 이번 제외 |
| AC28 | 사용자별 자료·승인·파일 접근 격리 | 다른 사용자 토큰/무인증 요청 접근 거부, DB 및 Storage RLS 확인 |

UI와 실제 연결 결과는 따로 보고한다. 설정 화면 시연을 위해 유료 SAML 요금제를 구매하거나 임의의 회사 IdP를 연결하지 않는다.

## 신규 입력 처리의 기능 검증 — 시연에도 동일 적용

P01/N01은 개발·회귀 기준선이며 시연을 이 자료의 저장된 결과로 제한하지 않는다. 시연 직전까지 해당 DB/Storage·검색 인덱스·지식·초안·분석 캐시에 없는 신규 합성 자료를 지원 형식으로 준비한다. 사용자/시연자가 실제 업로드하면 그 bytes에서 추출·분석을 수행한다. 파일명·프로젝트 ID별 고정 답안, 사전 지식화 또는 미리 만든 결과를 신규 분석으로 보여주지 않는다. 기존 과거 자료는 연결 대상으로 저장되어 있어도 된다.

시연 순서: 신규 프로젝트/대상 선택 → 파일 업로드 → 원본 저장 → MD/JSON 변환 → 신규 조건 추출 → 기존 자료 검색·관련 근거 연결 → 조건 차이·적용성/검토 필요 → 양쪽 출처 확인 → 초안 검토·승인 → 실제 XLSX 다운로드. Search/AI Insight 상태는 실제 실행 방식과 일치해야 한다. 관계 그래프를 구현했다면 새 자료와 근거 관계가 반영되어야 한다.

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC29 | DB에 없던 신규 파일의 실제 업로드부터 기존 자료 연결·승인·출력까지 작동 | 업로드 전 신규 SHA-256 부재 확인, 업로드 시각/작업 ID/새 source ID, MD·JSON·양쪽 SourceRef와 생성 파일 기록. 새로고침 후 저장 상태 재확인 |
| AC30 | 내용이 다른 신규 입력에 결과가 반응하고 근거 없는 연결을 강제하지 않음 | 프로젝트명/파일명 고정 분기 없이 수치·단위·계약/현장 조건을 바꾼 새 입력 검증. 관련 근거가 있는 항목은 실제 기존 자료와 연결; 없는 항목은 연결 없음/REVIEW_REQUIRED. 변경 영향 있는 비교·초안 갱신 및 기존 승인 무효화 |

검증용 신규 입력은 기존 원본을 덮어쓰지 않는 별도 합성 파일로 만들고 EVALUATOR 정답/holdout을 사용하지 않는다. 지원하지 않는 형식·스캔은 명시적 제한으로 처리한다. 최초 신규 입력의 분석은 캐시 미사용을 확인하고, 이후 동일 입력의 캐시/중복 처리는 정확히 표시한다. 사내 SSO/LLM 실제 연결 제외와 API 상태 표시 규칙은 유지한다. AC29–30을 통과하지 않으면 신규 입력 처리 기능과 P0 완료로 선언하지 않는다.

## 기능·UI 최신 결정

`docs/UX_DECISIONS.md`를 구현 계약으로 적용한다. 프로젝트 선택 시작, 데이터 관리의 명시적 등록 승인·자동 변환, 좌측 지식조회와 모드 탭, 지식 자료실 범위 필터·원본 열기·근거 관계, 버전이 있는 공통 양식을 구현한다. ITB는 주요정보 추출+과거 이슈 연결을 우선하고 Risk Register·심의 장표 초안을 이어서 구현한다. 과거 최빈 강도는 근거 조건이 충족된 참고값이며 자동 확정값이 아니다.

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC31 | 프로젝트 선택·자료 귀속·같은 인입 기능의 두 진입점 | 신규/기존 프로젝트 시작, 대상 없는 등록 차단, 데이터 관리·프로젝트 추가 경로 일치 |
| AC32 | 데이터 관리의 승인·자동 처리·정확한 최신 상태 | 변경/승인 대기/실패/권한 상실에서 녹색 금지; 확인 범위·시각, 실패 파일 재시도·중복/버전 처리 |
| AC33 | 지식조회 메인 화면·모드 구분·자료 범위 명확 | 좌측 진입과 맥락 유지, 전체/현재/관련 과거 필터와 관련 이유, 원문 권한/locator 확인 |
| AC34 | 공통 양식 버전 재사용·승인 버전 보호 | 1회 등록 후 여러 프로젝트 선택, 원본 보존·이전 버전 유지, 새 버전 적용 시 재승인 |
| AC35 | ITB 주요정보·과거 이슈와 실제 XLSX | 주요정보 시트+기존 ITB 비교 시트, 원문 수치/단위·개정·트리거 근거 연결 및 빈값 처리 |
| AC36 | 근거가 있는 리스크·강도 참고·대응 및 실제 XLSX | 비교 불가/동률/소수 사례/빈약 근거 강도 공란, 사례 중복 제거·평가 척도 유지, 점수 임의 생성 금지 |
| AC37 | 항목별 심의 초안과 승인 후 PPTX | 실제 양식 5개 항목, 항목별 근거/확인 필요·편집·승인·재개봉 |
| AC38 | 실자료의 의미 있는 관계 탐색 | 관계 유형/양쪽 근거 조회, 신규 업로드 반영, 가짜 관계/거리 기반 점수 금지 |


## 최신 사용자 확정 (2026-09-21, /goal v1.0)

GOAL.md와 ops/GOAL_INPUT.txt의 자체 완결형 원문이 현재 실행 계약이다. 필요한 프로젝트 범위 오픈소스·스킬·플러그인 설치는 이번 지시로 승인됐으며 개별 재승인 없이 설치·버전 기록·구동 검증한다. 각 의미 있는 작업 단위는 검증 후 로컬 Git 커밋하고 실패 체크포인트는 구분한다. Supabase 실제 DB/Storage/Auth/RLS 연결 검증은 필수다. 배포·공개 게시·데모/영상/발표자료·행사 제출은 추후 사용자 요청까지 실행 범위에서 제외한다. 제품 기능 완료까지 OMX 구현→실행/검토→실패 수정→재검증→기록/커밋/체크포인트 루프를 계속한다. 이 문단과 현재 사용자 지시가 이전 설치 승인·배포 운영 문구보다 우선한다.


## UI 수정 06 — 최신 사용자 지시

현재 UI 계약은 docs/UX_DECISIONS.md와 docs/ui-review/index.html이다. 지식조회로 명칭을 변경하고, 새 프로젝트 이름 아래 파일 업로드를 제공한다. ITB 분석표·심의장표·Risk Register의 초안·근거·검토·승인 흐름을 유지한다. 지식 자료실은 목록/관계 보기 전환 없이 기본 지식 연결 노드와 과거↔신규 비교 설명을 함께 보여준다. 데이터 관리에는 과거 프로젝트/문서 보유량과 새로 유입된 문서를 표시하고 실패/처리중 KPI와 등록 대상 섹션은 제거한다. 폴더 자료 확인·승인은 별도 작업에서 유지한다. 실제 양식 폴더는 data/REBUILD_INPUT_v1/REBUILD_INPUT_v1/03_OUTPUT_TEMPLATES이며 제공 7종을 표시한다.

이번 요청은 UI 수정이다. GOAL.md·ops/GOAL_INPUT.txt·OMX 목표는 사용자 요청에 따라 수정하지 않았으며 후속 /goal 개정 시 최신 UI 계약을 반영해야 한다. 기존 goal의 명칭·화면 지시보다 이번 UI 지시가 우선한다. 이 시안은 백엔드·Supabase·LLM·최종 파일 생성 완료의 증거가 아니다.


## AI 인증 분리 — v1.1 최신 계약
이 절이 기존 단일 API-key 및 AC23의 운영진 키 전용 표현보다 우선한다. 제품 AI Provider는 로컬 개발·로컬 데모 Codex OAuth, 배포용 OpenAI API key다. 배포 adapter의 실제 최소 호출 시험은 서버에서 수행할 수 있으며 Vercel 배포 자체는 이번 제품 goal에 포함하지 않는다. 사내 LLM·SSO의 실제 연결은 제외한다. 전체 제품 데이터 경로에 두 adapter를 연결해야 하며 server/ai 단독 smoke만으로 AC23/전체 제품 완료를 선언하지 않는다.

| ID | 합격 조건 | 검증 |
|---|---|---|
| AC39 | local/demo는 공식 Codex OAuth만 사용 | 공식 CLI 로그인 상태 + 실제 호출 + 근거 검증. OPENAI_API_KEY가 존재해도 로컬 호출에 사용되지 않음. 미로그인/만료/한도 오류를 정직하게 표시 |
| AC40 | deployed는 서버 OpenAI API key만 사용 | 실제 Responses 호출 + 결과 근거 검증. production/hosted에서 OAuth 경로 거부. 키/모델 미설정과 호출 실패 시 다른 인증으로 전환 금지 |
| AC41 | Provider·인증 경계 | 최소 자식 환경, 로컬 전용 실행·접근 제한, 문서 지시 무시, 비밀/토큰/헤더 출력 금지, 브라우저 전달 금지, 유효 SourceRef와 실패/시간 제한 검증 |
| AC42 | 재현 가능한 도구·장시간 운영·완료 판정 | 의존성 버전/출처, Windows 구동, 작업 커밋·원시 로그·checkpoint, 독립 코드/설계 검토와 OMX strict gate. 실제 입력/실행 없이 goal 활성화·완료로 기록하지 않음 |
