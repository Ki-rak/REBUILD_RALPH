'use strict';
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const inventory=window.UI_INVENTORY;
const state={page:'project',project:null,projects:[],output:'itb',mode:'search',query:'',relation:'water',drafts:{},slide:0,newFiles:[],fileTarget:'new',revision:0,dataExpanded:false,history:[],provider:'OpenAI'};
const paths={project:'M3 7h6l2 2h10v11H3z M3 7V4h6l2 3',search:'M21 21l-5-5 M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0',library:'M5 4h14v16H5z M9 8h6 M9 12h6 M9 16h3',data:'M4 5c0-4 16-4 16 0s-16 4-16 0v14c0 4 16 4 16 0V5 M4 12c0 4 16 4 16 0',templates:'M5 3h9l5 5v13H5z M14 3v6h5 M9 13h6 M9 17h6',settings:'M12 8a4 4 0 1 1 0 8 4 4 0 0 1 0-8 M12 2v3 M12 19v3 M2 12h3 M19 12h3 M5 5l2 2 M17 17l2 2 M5 19l2-2 M17 7l2-2',upload:'M12 16V3 M7 8l5-5 5 5 M4 15v6h16v-6',plus:'M12 5v14 M5 12h14',arrow:'M5 12h14 M14 7l5 5-5 5',itb:'M4 3h16v18H4z M4 9h16 M4 15h16 M10 9v12',risk:'M12 3L2 21h20z M12 9v5 M12 18h.01',slides:'M3 3h18v13H3z M12 16v5 M8 21h8 M7 11l4-3 3 2 3-4',link:'M9 15l6-6 M8 17l-2 2a4 4 0 0 1-5-5l4-4a4 4 0 0 1 5 0 M16 7l2-2a4 4 0 0 1 5 5l-4 4a4 4 0 0 1-5 0',check:'M4 12l5 5L20 6'};
function icon(name){return `<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[name]||paths.templates}"/></svg>`}
const menus=[['project','프로젝트','project'],['knowledge','지식조회','search'],['library','지식 자료실','library'],['data','데이터 관리','data'],['templates','표준 양식','templates'],['settings','설정','settings']];
const outputs=[{key:'itb',name:'ITB 분석표',sub:'입찰 조건과 과거 이슈',ext:'XLSX'},{key:'slides',name:'심의장표',sub:'핵심 쟁점과 의사결정',ext:'PPTX'},{key:'risk',name:'Risk Register',sub:'위험과 대응방안',ext:'XLSX'}];
const sources={
 p01water:{id:'p01water',project:'P01 · 해온 도시철도 2공구',name:'01_Project_Brief_and_Lessons.docx',where:'문단 11–15 · P01-LL01',status:'Rev01 · APPROVED',quote:'S2 정거장 굴착 중 담수 유입량이 35에서 60 m3/h로 증가했다. 기존 집수조의 침전 시간이 부족해 방류가 6일 중단됐다.\n염수 현장에서는 담수 침전 방식만 적용할 수 없다.',url:'sources/P01_01_Project_Brief_and_Lessons.docx',note:'문단 11과 15의 발췌입니다. 생략된 대응 과정은 원본에서 확인하세요.'},
 p01notice:{id:'p01notice',project:'P01 · 해온 도시철도 2공구',name:'01_Project_Brief_and_Lessons.docx',where:'문단 27–31 · P01-LL03',status:'Rev01 · APPROVED',quote:'변경 관련 추가원가 4.8 USD million 중 3.1 USD million이 승인되었다. 미승인분은 시공자 부담으로 남았다.\n본 사업의 통지기한은 28일이다. 신규 사업의 기한과 우선문서가 확인되면 그대로 재사용하지 않는다.',url:'sources/P01_01_Project_Brief_and_Lessons.docx',note:'금액 승인을 지연 원인이나 통지 위반의 확정 결과로 단정하지 않습니다.'},
 n01site:{id:'n01site',project:'N01 · 해온 블루라인 4공구',name:'02_ITB_and_Contract_Rev00.pdf',where:'3페이지 · G1 / G2 / G3',status:'Rev00 · APPROVED',quote:'풍화 암반 / 염수 혼재. 염화물 2500 mg/L.\n설계용 지하수 유입량 검토 중 / 확정치 없음. 허가량으로 펌프 용량을 정하지 않는다.',url:'sources/N01_02_ITB_and_Contract_Rev00.pdf#page=3',note:'설계 유입량 미확정과 방류 허용량을 구분한 원문 요약입니다.'},
 n01add:{id:'n01add',project:'N01 · 해온 블루라인 4공구',name:'11_Addendum_Rev01.pdf',where:'1페이지 · 조항 20.1 / ENV 4',status:'Rev01 · APPROVED',quote:'변경 통지기한 14 calendar days.\n방류 허용량 450 m3/day. 24시간 기준이다. 설계 펌프용량을 대신 확정하지 않는다.',url:'sources/N01_11_Addendum_Rev01.pdf#page=1',note:'승인된 변경 항목만 ITB Rev00보다 우선합니다. Rev02는 FOR REVIEW 상태입니다.'},
 n01overview:{id:'n01overview',project:'N01 · 해온 블루라인 4공구',name:'02_ITB_and_Contract_Rev00.pdf',where:'1페이지 · 1.1–1.4',status:'Rev00 · APPROVED',quote:'발주처 해온 도시교통청 · 설계시공 일괄\n계획 공기 42개월 · 계획 착공 2027-02-01 · 예정 준공 2030-07-15\n발주처 예산한도 365 USD million · JV 전체 · 당사 지분 55%',url:'sources/N01_02_ITB_and_Contract_Rev00.pdf#page=1',note:'예산한도는 확정 계약금액이나 수익이 아닙니다. 공기와 달력 날짜는 원문 그대로 구분합니다.'}
};
const relations=[
 {id:'water',title:'수처리 방식의 적용 조건',short:'수처리 조건',pastNode:'담수 유입 · 방류 중단',newNode:'염수 혼재 · 2,500 mg/L',past:'p01water',current:'n01site',pastTitle:'담수 현장의 처리 경험',newTitle:'염수 조건의 신규 현장',pastText:'P01은 유입 증가와 침전 시간 부족으로 방류가 6일 중단됐습니다. 담수 현장의 대응 경험입니다.',newText:'N01은 염화물 2,500 mg/L의 염수 혼재 조건입니다. 설계 유입량도 아직 확정되지 않았습니다.',status:'조건 차이 · 검토 필요',reason:'지하수 처리라는 업무는 같지만 수질 조건이 다릅니다. P01의 담수 침전 방식과 설비 용량을 N01에 그대로 적용할 수 없습니다.',action:'현장 수질·처리 공정·설계 유입량을 확인한 뒤 대응방안을 검토합니다.',outputs:['risk','itb','slides']},
 {id:'notice',title:'변경 통지기한과 권리 관리',short:'계약 통지',pastNode:'과거 통지기한 · 28일',newNode:'승인 정정서 · 14일',past:'p01notice',current:'n01add',pastTitle:'28일 기준의 과거 계약',newTitle:'14일로 변경된 신규 조건',pastText:'P01 교훈은 통지 절차와 승인·미승인 금액의 구분을 강조합니다. 과거 통지기한은 28일입니다.',newText:'N01 승인 정정서 Rev01은 통지기한을 14 calendar days로 변경합니다.',status:'조건 변경 · 재검토',reason:'과거 일정표를 재사용하면 신규 기한을 놓칠 수 있습니다. P01의 기록 관리 방법은 참고하되 N01의 승인 문서에 맞춰 통지 일정을 다시 설정해야 합니다.',action:'14일 기한과 수신인·발송 증빙·기산일을 검토하고 질의 사항을 남깁니다.',outputs:['itb','risk','slides']},
 {id:'capacity',title:'방류 허용량과 설계 유입량의 구분',short:'설계값 확인',pastNode:'관측 유입량 · 35 → 60 m³/h',newNode:'허용량 · 450 m³/day',past:'p01water',current:'n01add',pastTitle:'과거 현장에서 관측한 유입량',newTitle:'새 프로젝트의 방류 허용량',pastText:'P01의 35→60 m3/h는 과거 현장의 관측 기록입니다. N01 설계값이 아닙니다.',newText:'N01의 450 m3/day는 승인 정정서의 방류 허용량입니다. 설계 펌프용량을 뜻하지 않습니다.',status:'설계값 미확정',reason:'관측 유입량·방류 허용량·펌프 설계용량은 서로 다른 값입니다. 단위를 바꾸거나 과거 값을 복사하는 것만으로 신규 설비 용량을 확정할 수 없습니다.',action:'추가 조사와 설계 검토 결과를 확보할 때까지 설계값은 빈칸으로 둡니다.',outputs:['itb','risk']}
];
let focusBack=null,toastTimer;
const projectSessions=new Map();
const selectedRelation=()=>relations.find(r=>r.id===state.relation)||relations[0];
const currentDraft=()=>state.drafts[state.output];
const templateFor=key=>inventory.templates.find(t=>t.key===key);
function toast(text){$('#toast').textContent=text;$('#toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').hidden=true,4500)}
function btn(action,text,kind='',symbol=''){return `<button type="button" class="btn ${kind}" data-action="${action}">${symbol?icon(symbol):''}${text}</button>`}
function evidence(id,text='원문 근거'){return `<button class="link" data-evidence="${id}">${text} ↗</button>`}
function head(title,desc,actions='',eyebrow='WORKSPACE'){return `<header class="pagehead"><div><div class="eyebrow">${eyebrow}</div><h1>${esc(title)}</h1><p>${desc}</p></div><div class="row">${actions}</div></header>`}
function footer(){return '<footer class="footer"><span>RE:Build Agent</span><span>제공 자료 기반 UI 시안 · 실제 분석 결과와 구분합니다.</span></footer>'}
function navigate(page){clearTimeout(toastTimer);$('#toast').hidden=true;state.page=page;render();window.scrollTo(0,0)}
function projectHome(){const recent=[...state.projects,...inventory.projects.filter(p=>p.kind==='new')].map(p=>projectSessions.get(p.id)||p);return head('프로젝트','새로운 프로젝트에, 축적된 경험을 연결하세요.',btn('new-project','새 프로젝트','primary','plus'))+`<section class="intro"><div class="eyebrow">FROM EXPERIENCE TO EVIDENCE</div><h2>과거의 경험에서 찾고,<br>근거를 확인하며 완성하세요.</h2><p>프로젝트 자료를 추가하면 관련 사례를 찾고<br>ITB 분석표, 심의장표, Risk Register를 함께 검토할 수 있습니다.</p><div class="row">${btn('new-project','프로젝트 시작하기','dark','arrow')}<button class="link" data-page="library">지식 자료실 둘러보기 ↗</button></div></section><div class="sectiontitle"><h2>검토 프로젝트</h2><span>제공 자료 ${inventory.projects.filter(p=>p.kind==='new').length}개 · 이번 화면에서 만든 프로젝트 ${state.projects.length}개</span></div><div class="projectcards">${recent.map(p=>`<button class="projectcard" data-open-project="${esc(p.id)}"><div class="row between"><span class="projectbadge">${esc(p.id.startsWith('UI-')?'NEW':p.id)}</span><span class="tag">${p.kind==='new'?'제공 프로젝트':'이번 화면에서 생성'}</span></div><h3>${esc(p.name)}</h3><p>${p.kind==='new'?'제공된 자료로 검토 흐름을 확인합니다.':'추가한 문서를 바탕으로 검토할 프로젝트입니다.'}</p><div class="projectmeta"><span>문서 ${p.files.length}개</span><span>프로젝트 열기 ↗</span></div></button>`).join('')}</div>`}
function project(){if(!state.project)return projectHome();const p=state.project,draft=currentDraft(),o=outputs.find(x=>x.key===state.output);return head(p.name,'자료에서 근거를 확인하고, 검토한 내용으로 결과물을 완성합니다.',btn('switch-project','프로젝트 목록')+btn('upload','자료 추가','primary','upload'),`${esc(p.id)} / PROJECT`)+`<div class="intakebar">${icon('project')}<div class="grow"><h3>프로젝트 자료 <span class="tag">${p.files.length}개</span></h3><p>${p.kind==='new'?'제공 원본 자료 · 분석 실행 전':'추가한 파일명 확인 · 실제 저장 및 분석 연결 전'}</p></div>${btn('project-files','자료 확인')}<div class="flow"><span>원본 보존</span><span>지식화</span><span>근거 연결</span></div></div><div class="outputnav" aria-label="업무 결과물">${outputs.map(x=>`<button class="outputtab ${state.output===x.key?'active':''}" data-output="${x.key}" aria-pressed="${state.output===x.key}">${icon(x.key)}<span><strong>${x.name}</strong><small>${x.sub}</small></span></button>`).join('')}</div><section class="card"><div class="cardhead"><div><h2>${o.name}</h2><p>${templateFor(o.key).name} · 제공 양식</p></div><div class="row"><span class="tag ${draft?.approved?'green':'amber'}">${draft?(draft.approved?'승인됨 · 예시':'검토 중 · 예시'):'초안 작성 전'}</span><button class="link" data-page="templates">양식 확인 ↗</button></div></div><div class="steps">${['자료 확인','초안 작성','근거 검토','검토·승인','파일 생성'].map((t,i)=>`<span class="${i===(draft?(draft.approved?3:2):0)?'active':''}"><b>${i+1}</b>${t}</span>`).join('')}</div><div class="outputbody">${draft?draftView():`<div class="outputempty"><div><h3>${o.key==='itb'?'주요 계약 조건부터,<br>놓치기 쉬운 과거 이슈까지.':o.key==='risk'?'새로운 프로젝트의 위험을<br>과거 사례와 함께 검토합니다.':'심의에 필요한 판단과 근거를<br>다섯 장에 담습니다.'}</h3><p>${o.key==='itb'?'공기·발주처·계약 방식 등 핵심 정보를 확인하고, 과거 프로젝트에서 문제가 됐던 조건을 함께 연결합니다.':o.key==='risk'?'위험과 발생 조건, 과거 적용 사례, 대응방안을 나란히 검토합니다. 근거가 부족한 강도는 미확정으로 남깁니다.':'사업 개요, ITB 주요 조건, 과거 적용성, 위험 대응, 의사결정 항목을 순서대로 검토합니다.'}</p>${btn('generate','예시 초안 열기','primary','plus')}<p class="fieldhint">제공 N01 자료의 화면 예시입니다. 새로 올린 파일의 분석 결과가 아닙니다.</p></div><div class="checklist"><span>원문 위치가 연결된 주요 정보</span><span>과거 조건과 신규 조건의 비교</span><span>미확정 사항을 구분하는 검토</span><span>수정 · 승인 · 파일 생성</span></div></div>`}</div>${draft?`<div class="draftactions row between"><div><p>제공 N01 자료를 활용한 검토 흐름 예시</p><p>실제 ${o.ext} 생성은 분석·저장 연결 후 가능합니다.</p></div><div class="row">${btn('edit-draft','내용 수정')}${btn('approve-draft',draft.approved?'승인 내역':'검토 후 승인',draft.approved?'':'primary')}<button class="btn" disabled title="제품 분석·저장 연결 후 제공">${o.ext} 파일 생성</button></div></div>`:''}</section>`}
function draftView(){const d=currentDraft();let body='';if(state.output==='itb')body=`<div class="row between" style="margin-bottom:15px"><span class="tag green">주요 정보 · 제공 N01 예시</span><span class="small muted">XLSX 구성: 주요정보 + ITB 상세 비교</span></div><div class="tablewrap"><table><thead><tr><th>주요 항목</th><th>신규 프로젝트 조건</th><th>연결된 과거 이슈</th><th>근거</th></tr></thead><tbody><tr><td>발주처 / 계약 방식</td><td>해온 도시교통청<p>설계시공 일괄</p></td><td>계약별 책임 경계 확인</td><td>${evidence('n01overview')}</td></tr><tr><td>계획 공기</td><td>42개월<p>예정 준공 2030-07-15</p></td><td>일정 영향은 별도 검토</td><td>${evidence('n01overview')}</td></tr><tr><td>변경 통지기한</td><td>14 calendar days<p>승인 정정서 Rev01 우선</p></td><td><button class="link" data-relation-link="notice">P01 · 과거 28일과 차이 ↗</button></td><td>${evidence('n01add')}</td></tr><tr><td>방류 허용량</td><td>450 m3/day<p>설계 펌프용량과 구분</p></td><td><button class="link" data-relation-link="water">P01 · 담수 처리 경험 ↗</button></td><td>${evidence('n01add')}</td></tr><tr><td>설계 유입량</td><td>— <span class="tag amber">검토 필요</span></td><td><button class="link" data-relation-link="capacity">과거 관측값 직접 적용 불가 ↗</button></td><td>${evidence('n01site')}</td></tr></tbody></table></div><details style="margin-top:17px"><summary class="link">상세 비교 시트의 8개 항목 확인</summary><p class="small muted">항목 ID · ITB 조항 · 신규 조건 · 과거 사례 · 조건 차이 · 적용 판단 · 원문 근거 · 확인 필요 사항</p></details>`;
else if(state.output==='risk')body=`<div class="row between" style="margin-bottom:15px"><span class="tag green">리스크 초안 · 제공 N01 예시</span><span class="small muted">강도 미확정 · 독립 사례와 동일 척도 확인 필요</span></div><div class="tablewrap"><table><thead><tr><th>리스크 / 발생 조건</th><th>강도</th><th>과거 적용 프로젝트</th><th>대응방안</th><th>근거</th></tr></thead><tbody><tr><td>수처리 방식 부적합<p>담수 경험을 염수 현장에 직접 적용</p></td><td>—<p>검토 필요</p></td><td>P01 · 해온 도시철도 2공구</td><td>수질·유입량 확인 후 처리 방식 재검토</td><td><button class="link" data-relation-link="water">양쪽 근거 ↗</button></td></tr><tr><td>변경 통지기한 오적용<p>과거 28일 절차의 재사용</p></td><td>—<p>검토 필요</p></td><td>P01 · 계약 통지 교훈</td><td>14일 기한에 맞춰 기록·증빙 절차 검토</td><td><button class="link" data-relation-link="notice">양쪽 근거 ↗</button></td></tr></tbody></table></div><div class="note">동일 프로젝트의 중복 문서는 독립 사례로 세지 않습니다. 비교 가능한 강도 근거가 없으므로 발생가능성·영향·점수는 빈칸입니다.</div>`;
else{const slides=[['사업 개요와 심의 요청',['해온 블루라인 도시철도 4공구','발주처 해온 도시교통청 · 설계시공 일괄','입찰 참여 조건 및 추가 확인 사항 심의'],'n01overview'],['ITB 주요 조건',['계획 공기 42개월 · JV 전체 예산한도 USD 365 million','승인 정정서: 통지기한 14 calendar days','방류 허용량 450 m3/day'],'n01add'],['과거 사례와 적용성',['P01의 지하수 처리 경험과 비교','담수 → 염수 혼재: 직접 적용 불가','공정·용량·허가 조건의 차이 확인 필요'],'p01water'],['우선 위험과 대응',['수처리 방식의 현장 적합성 검토','통지기한 차이를 반영한 계약 관리','강도 미확정 · 근거 확보 후 검토'],'p01notice'],['미확정 사항과 의사결정',['설계 유입량 · 펌프 용량 확정 근거 필요','처리 공정과 방류 조건의 적합성 확인','질의 및 의사결정 항목을 검토 후 승인'],'n01site']];const s=slides[state.slide];body=`<div class="slidelayout"><div class="slidenav" aria-label="심의장표 페이지">${slides.map((s,i)=>`<button data-slide="${i}" class="${i===state.slide?'active':''}" aria-pressed="${i===state.slide}">0${i+1} · ${s[0]}</button>`).join('')}</div><article class="slide"><div class="eyebrow">N01 / REVIEW DRAFT · ${state.slide+1} / 5</div><h3>${s[0]}</h3><ul>${s[1].map(x=>`<li>${x}</li>`).join('')}</ul><div class="slidefoot row between"><span>RE:Build Agent · 검토용 예시 초안</span>${evidence(s[2],'이 장표의 근거')}</div></article></div>`}
return body+(d.note?`<div class="reviewmemo"><strong>검토 의견</strong><p>${esc(d.note)}</p></div>`:'')}
function knowledge(){const found=Object.values(sources).filter(s=>(s.name+s.project+s.quote+s.where).toLowerCase().includes(state.query.toLowerCase()));return head('지식조회','과거의 자료를 찾고, 현재 프로젝트에 적용할 수 있는지 검토합니다.','','KNOWLEDGE SEARCH')+`<div class="row between"><div class="tabs" aria-label="지식조회 모드"><button data-mode="search" class="${state.mode==='search'?'active':''}" aria-pressed="${state.mode==='search'}">자료 찾기 · Search Mode</button><button data-mode="insight" class="${state.mode==='insight'?'active':''}" aria-pressed="${state.mode==='insight'}">적용 검토 · AI Insight Mode</button></div><span class="tag">${esc(state.project?.name||'제공 자료 범위')}</span></div><section class="searchhero"><div class="searchmark">${icon(state.mode==='search'?'search':'link')}</div><h2>${state.mode==='search'?'어떤 경험을 찾고 계신가요?':'이 경험을 지금도 적용할 수 있을까요?'}</h2><p>${state.mode==='search'?'주요 조건, 과거 이슈, 원문 위치를 함께 찾아보세요.':'과거 조건과 새로운 조건의 차이를 근거와 함께 확인합니다.'}</p><form id="queryform"><label for="query" class="small">${state.mode==='search'?'찾을 자료 또는 키워드':'검토할 질문'}</label><div class="querybox"><input id="query" type="search" value="${esc(state.query)}" placeholder="예: 지하수 처리, 변경 통지기한, 방류 허용량"><button class="btn primary" ${state.mode==='insight'?'disabled':''}>${state.mode==='search'?'예시 자료 검색':'AI 연결 필요'}</button></div></form><div class="suggestions">${['지하수','통지기한','방류','공기'].map(q=>`<button data-query="${q}">${q}</button>`).join('')}</div></section>${state.mode==='search'?`<div class="sectiontitle"><h2>근거 자료 <span class="tag">${found.length}</span></h2><span>제공 원문의 요약 검색 · 신규 업로드 분석 전</span></div><div class="grid2">${found.length?found.map(s=>`<article class="resultcard"><div class="resultmeta"><span>${s.project}</span><span>${s.status}</span></div><h3>${s.id==='n01add'?'승인 정정서의 유효 조건':s.id==='p01water'?'지하수 유입과 방류 중단 교훈':s.id==='p01notice'?'변경 통지와 승인 금액 관리':s.id==='n01site'?'신규 현장 수질과 설계 조건':'신규 사업의 주요 정보'}</h3><p>${esc(s.quote).replace(/\n/g,'<br>')}</p><div class="row between"><span class="small muted">${s.where}</span>${evidence(s.id)}</div></article>`).join(''):'<div class="empty">일치하는 제공 자료가 없습니다.</div>'}</div>`:`<section class="card"><div class="empty"><strong>OpenAI 연결 확인이 필요합니다</strong><p>연결 후 신규 조건 · 과거 사례 · 적용성 · 부족한 근거를 검토합니다.</p><button class="link" data-page="settings">AI 연결 설정 →</button></div></section>`}`}
function library(){const r=selectedRelation();return head('지식 자료실','자료가 연결되는 이유와, 그대로 적용할 수 없는 차이까지 확인합니다.',`<button class="btn" data-page="data">${icon('data')}보유 자료 현황</button>`,'CONNECTED KNOWLEDGE')+`<section class="card graphcard"><div class="graphhead"><div><h2>지식 연결 지도</h2><p>P01 해온 도시철도 2공구 ↔ N01 해온 블루라인 4공구</p></div><span class="tag green">제공 원문 기반 · 비교 예시</span></div><div class="graph"><div class="graphlabels"><span>과거 프로젝트 · 경험</span><span>연결되는 검토 항목</span><span>신규 프로젝트 · 조건</span></div>${relations.map(x=>`<div class="graphrow ${x.id===state.relation?'selected':''}"><button class="node" data-relation="${x.id}" aria-pressed="${x.id===state.relation}"><small>P01 · 과거 자료</small><strong>${x.pastNode}</strong></button><button class="node condition" data-relation="${x.id}" aria-pressed="${x.id===state.relation}"><strong>${x.short}</strong><small>조건 비교 · 근거 연결</small></button><button class="node" data-relation="${x.id}" aria-pressed="${x.id===state.relation}"><small>N01 · 신규 자료</small><strong>${x.newNode}</strong></button></div>`).join('')}</div><div class="graphlegend"><span><i class="legenddot"></i>노드를 선택하면 아래에서 양쪽 조건을 비교합니다.</span><span>연결은 유사도 점수나 적용 확정을 뜻하지 않습니다.</span></div></section><section class="relations"><div class="sectiontitle"><h2>과거와 신규, 어떻게 연결되나요?</h2><span>근거 · 차이 · 다음 검토</span></div><div class="relationtabs" aria-label="지식 연결 항목">${relations.map(x=>`<button data-relation="${x.id}" class="${x.id===r.id?'active':''}" aria-pressed="${x.id===r.id}">${x.short}</button>`).join('')}</div><article class="card"><div class="cardhead"><h2>${r.title}</h2><span class="tag amber">${r.status}</span></div><div class="comparison"><section class="sourcebox"><div class="eyebrow">PAST · P01</div><h3>${r.pastTitle}</h3><p>${r.pastText}</p><div class="sourceloc">${sources[r.past].where}</div>${evidence(r.past,'과거 원문 확인')}</section><div class="bridge" aria-hidden="true">↔</div><section class="sourcebox new"><div class="eyebrow">NEW · N01</div><h3>${r.newTitle}</h3><p>${r.newText}</p><div class="sourceloc">${sources[r.current].where}</div>${evidence(r.current,'신규 원문 확인')}</section></div><div class="conclusion"><h3>연결의 의미</h3><div><p>${r.reason}</p><div class="actionline">다음 검토 → ${r.action}</div></div></div><div class="relatedoutputs"><span>연결할 업무</span>${r.outputs.map(k=>`<button data-to-output="${k}">${outputs.find(o=>o.key===k).name} ↗</button>`).join('')}</div></article></section><div class="sectiontitle"><h2>연결에 사용한 자료</h2><span>과거·신규 출처와 위치를 함께 보존</span></div><section class="card">${sourceTable([sources[r.past],sources[r.current]])}</section>`}
function sourceTable(list){return `<div class="tablewrap"><table><thead><tr><th>프로젝트</th><th>원본 자료</th><th>위치 / 개정</th><th>확인</th></tr></thead><tbody>${list.map(s=>`<tr><td>${s.project}</td><td>${s.name}</td><td>${s.where}<p>${s.status}</p></td><td>${evidence(s.id)}</td></tr>`).join('')}</tbody></table></div>`}
function data(){const past=inventory.projects.filter(p=>p.kind==='past'),total=inventory.pastCount,types=Object.entries(inventory.types).sort((a,b)=>b[1]-a[1]);return head('데이터 관리','축적된 과거 자료의 규모와 새로 유입된 문서를 한곳에서 관리합니다.',btn('folder','수동 업데이트','','upload'),'DATA COLLECTION')+`<div class="stats"><div class="stat"><span class="label">과거 프로젝트</span><strong>${past.length}<small>개</small></strong><p>축적된 프로젝트 자료 묶음</p></div><div class="stat"><span class="label">과거 보유 문서</span><strong>${total}<small>개</small></strong><p>제공된 과거 원본 파일</p></div><div class="stat"><span class="label">새로 유입된 문서</span><strong>${inventory.newCount}<small>개</small></strong><p>제공 신규 프로젝트 2개 기준</p></div><div class="stat"><span class="label">공통 표준 양식</span><strong>${inventory.templates.length}<small>개</small></strong><p>프로젝트 간 재사용 가능</p></div></div><div class="note">제공 폴더를 실제 집계한 현황입니다. Supabase 등록·동기화 상태는 연결 후 확인합니다. 이번 화면에서 추가한 파일은 원본 저장 전입니다.</div><div class="dataoverview"><section class="card"><div class="cardhead"><div><h2>과거 프로젝트 보유 현황</h2><p>프로젝트별 원본 문서와 파일 형식</p></div><span class="tag">${past.length}개 프로젝트</span></div><div class="tablewrap"><table><thead><tr><th>프로젝트</th><th>보유 문서</th><th>자료 구성</th><th>확인</th></tr></thead><tbody>${(state.dataExpanded?past:past.slice(0,6)).map(p=>`<tr><td><span class="small muted">${p.id}</span><br>${esc(p.name)}</td><td>${p.count}개</td><td><span class="small muted">${[...new Set(p.files.map(f=>f.type))].join(' · ')}</span></td><td><button class="link" data-inventory="${p.id}">자료 확인</button></td></tr>`).join('')}</tbody></table></div><button class="link" data-action="expand-data" style="margin-top:15px">${state.dataExpanded?'접기 ↑':'15개 프로젝트 모두 보기 ↓'}</button></section><aside class="stack dataaside"><section class="card"><div class="cardhead"><h2>문서 구성</h2><span class="tag">${total}개</span></div>${types.map(([type,n])=>`<div class="barrow"><span>${type}</span><div class="bartrack"><div style="width:${n/total*100*2.8}%"></div></div><span>${n}</span></div>`).join('')}<p class="small muted" style="margin-top:15px">원본 형식을 유지하며 지식으로 활용합니다.</p></section><section class="card"><h2>새 유입 자료</h2><ul class="timeline" style="padding:0;margin:20px 0 0">${inventory.projects.filter(p=>p.kind==='new').map(p=>`<li><strong>${p.id} · ${esc(p.name)}</strong><p>제공 원본 ${p.count}개 · 신규 프로젝트 자료</p><button class="link" data-inventory="${p.id}">문서 확인 ↗</button></li>`).join('')}${state.history.map(h=>`<li><strong>${esc(h.name)}</strong><p>${h.count}개 · ${h.time} · 화면에 추가, 저장 전</p></li>`).join('')}</ul></section></aside></div><p class="small muted" style="margin-top:17px">제공 폴더 집계: ${new Date(inventory.checkedAt).toLocaleString('ko-KR')} · 자동 동기화 미연결</p>`}
function templatesPage(){const core=outputs.map(o=>templateFor(o.key)),extra=inventory.templates.filter(t=>!outputs.some(o=>o.key===t.key));const card=t=>`<article class="templatecard"><div class="templateart" aria-hidden="true"><div class="sheet">${'<span></span>'.repeat(6)}</div></div><div class="row between"><h3>${t.label}</h3><span class="tag">${t.name.split('.').pop().toUpperCase()}</span></div><p>${t.description}</p><span class="filename">${t.name}</span><div class="templatefoot" style="margin-top:20px"><a class="link" href="${t.url}" download>원본 다운로드 ↙</a>${outputs.some(o=>o.key===t.key)?`<button class="btn" data-template="${t.key}">프로젝트에 사용</button>`:'<span class="tag">추가 업무 양식</span>'}</div></article>`;return head('표준 양식','제공된 원본 양식으로 시작하고, 프로젝트에 맞는 버전을 선택합니다.','','OUTPUT TEMPLATES')+`<div class="row between"><h2>기본 업무 양식</h2><span class="tag green">실제 제공 양식 ${inventory.templates.length}개</span></div><p class="small muted" style="margin-top:10px">확인된 양식 폴더</p><div class="path">${inventory.templateRoot}</div><div class="templategrid">${core.map(card).join('')}</div><div class="sectiontitle"><h2>함께 제공된 양식</h2><span>공통 양식으로 누적 관리</span></div><div class="templategrid">${extra.map(card).join('')}</div><div class="note">현재 제공 원본 버전을 표시합니다. 실제 서비스에서는 새 버전을 보존하고, 프로젝트가 선택한 양식과 승인된 초안을 함께 고정합니다.</div>`}
function settings(){return head('설정','자료를 보관하고 활용하는 방식, AI 연결과 로그인 환경을 설정합니다.','','WORKSPACE SETTINGS')+`<div class="settingsgrid"><section class="card"><div class="row between"><h2>자료 보관 위치</h2><span class="tag amber">연결 미확인</span></div><p>원본, 변환 자료와 결과물을 보관합니다.</p><label>저장 서비스</label><div class="row">${icon('data')}Supabase Storage</div><p>사용자에게 허용된 자료만 조회할 수 있습니다.</p></section><section class="card"><h2>지식 활용 범위</h2><p>검토에 사용할 자료의 기본 범위입니다.</p><label for="knowledge-scope">참조 범위</label><select id="knowledge-scope"><option>현재 프로젝트 + 접근 가능한 공통 자료</option><option>현재 프로젝트만</option></select></section><section class="card"><div class="row between"><h2>AI 연결</h2><span class="tag amber">연결 미검증</span></div><p>지식조회의 적용 검토에 사용할 공급자를 설정합니다.</p><label for="provider">API Provider</label><select id="provider"><option ${state.provider==='OpenAI'?'selected':''}>OpenAI</option><option ${state.provider==='사내 LLM'?'selected':''}>사내 LLM</option></select><label for="model-name">모델</label><input id="model-name" type="text" placeholder="허용된 모델 ID"><p>비밀키는 안전한 서버 설정에서 관리합니다. 이 UI 시안에서는 입력받지 않습니다.</p><button class="btn" disabled style="margin-top:15px">연결 시험 · 서버 연결 필요</button></section><section class="card"><h2>로그인 · SSO</h2><p>기본 로그인과 사내 인증 방식을 관리합니다.</p><label>기본 로그인</label><span class="tag">Supabase Auth · 연결 미확인</span><label for="sso-type">사내 SSO</label><select id="sso-type"><option>미설정 · 추후 결정</option><option>Microsoft Entra ID</option><option>Google Workspace</option><option>SAML</option></select></section></div>`}
function render(){const views={project,knowledge,library,data,templates:templatesPage,settings};$('#nav').innerHTML=menus.map(([id,label,symbol])=>`<button class="mainnav ${id===state.page?'active':''}" data-page="${id}" aria-current="${id===state.page?'page':'false'}">${icon(symbol)}${label}</button>`).join('');$('#main').innerHTML=views[state.page]()+footer();$('#topcontext').textContent=menus.find(m=>m[0]===state.page)[1];$('#currentproject').innerHTML=`<div class="eyebrow">CURRENT PROJECT</div><strong>${esc(state.project?.name||'프로젝트 선택 전')}</strong>${state.project?'<button class="link" data-page="project">검토 이어가기 →</button>':'<p class="fieldhint">새 프로젝트를 만들거나<br>기존 프로젝트를 여세요.</p>'}`}
function modal(title,content){focusBack=document.activeElement;$('#modaltitle').textContent=title;$('#modalbody').innerHTML=content;$('#modalback').hidden=false;$('#app').inert=true;$('#modalback button').focus()}
function close(){const origin=focusBack;$('#modalback').hidden=true;$('#panelback').hidden=true;$('#app').inert=false;if(origin?.isConnected)origin.focus();else $('#main h1')?.focus()}
function showEvidence(id){const s=sources[id];if(!s)return;focusBack=document.activeElement;$('#panelbody').innerHTML=`<span class="tag green">제공 원문</span><h3>${esc(s.name)}</h3><p>${s.project}</p><div class="row" style="margin-top:12px"><span class="tag">${s.status}</span><span class="small muted">${s.where}</span></div><div class="quote">${esc(s.quote)}</div><p>${s.note}</p><div class="row" style="margin-top:24px"><a class="btn primary" href="${s.url}" target="_blank" rel="noopener">원본 열기 ↗</a><a class="btn" href="${s.url.split('#')[0]}" download>다운로드</a></div><div class="note">제공된 합성 프로젝트 자료입니다. 실제 업로드 분석이 아닌 UI 검토용 근거입니다.</div>`;$('#panelback').hidden=false;$('#app').inert=true;$('#panelback button').focus()}
function fileList(files){return files.map((f,i)=>`<div class="filechip"><span>${icon('templates')} ${esc(f.name)} <small class="muted">${Math.max(1,Math.ceil(f.size/1024))} KB</small></span><button type="button" data-remove-file="${i}" aria-label="${esc(f.name)} 제거">×</button></div>`).join('')}
function uploadArea(){return `<div class="dropzone" id="dropzone">${icon('upload')}<h3>프로젝트 자료를 추가하세요</h3><p>파일을 끌어 놓거나 선택하세요.<br>PDF · DOCX · XLSX · PPTX · MD · TXT · CSV · EML</p>${btn('pick-files','파일 선택','','plus')}</div><div id="filechips" class="filechips">${fileList(state.newFiles)}</div><p class="err" id="fileerror" role="status"></p><p class="fieldhint">UI 시안에서는 파일명과 크기만 확인하며, 원본을 저장하거나 전송하지 않습니다.</p>`}
function newProject(){state.newFiles=[];state.fileTarget='new';modal('새 프로젝트 만들기',`<form id="newprojectform"><label for="project-name">프로젝트 이름</label><input id="project-name" type="text" required maxlength="80" placeholder="예: 해온 블루라인 4공구 검토"><label>프로젝트 자료 <span class="muted small">선택 · 나중에 추가 가능</span></label>${uploadArea()}<div class="flow"><span>프로젝트 생성</span><span>원본 보존</span><span>지식화</span><span>검토</span></div><div class="modalfoot"><button class="btn" type="button" data-action="close">취소</button><button class="btn primary" type="submit">프로젝트 만들기</button></div></form>`);$('#project-name').focus()}
function addFiles(list){const allow=/\.(pdf|docx|xlsx|pptx|md|txt|csv|eml)$/i;let skipped=0;for(const file of list){if(!allow.test(file.name)){skipped++;continue}if(!state.newFiles.some(f=>f.name===file.name&&f.size===file.size&&f.lastModified===file.lastModified))state.newFiles.push({name:file.name,size:file.size,lastModified:file.lastModified})}if($('#filechips'))$('#filechips').innerHTML=fileList(state.newFiles);if($('#fileerror'))$('#fileerror').textContent=skipped?`지원 형식이 아닌 ${skipped}개 파일을 제외했습니다.`:'';$('#files').value=''}
function openProject(id){const p=state.projects.find(x=>x.id===id)||inventory.projects.find(x=>x.id===id);if(!p)return;if(!projectSessions.has(id))projectSessions.set(id,p.kind==='new'?structuredClone(p):p);state.project=projectSessions.get(id);state.drafts=state.project.reviewDrafts||{};state.project.reviewDrafts=state.drafts;state.revision=state.project.revision||0;navigate('project')}
function useTemplate(key){if(!state.project){toast('먼저 양식을 사용할 프로젝트를 선택하세요.');navigate('project');return}state.output=key;navigate('project')}
function inventoryModal(id){const p=inventory.projects.find(x=>x.id===id);if(!p)return;modal(`${p.id} · 보유 문서`, `<p class="muted" style="margin-top:18px">${esc(p.name)} · 제공 폴더 원본 ${p.count}개</p><div class="path">${esc(p.folder)}</div><div class="tablewrap"><table><thead><tr><th>문서명</th><th>형식</th><th>크기</th></tr></thead><tbody>${p.files.map(f=>`<tr><td>${esc(f.name)}</td><td>${f.type}</td><td>${Math.ceil(f.size/1024)} KB</td></tr>`).join('')}</tbody></table></div>`)}
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b||b.disabled)return;
 if(b.dataset.page){navigate(b.dataset.page);return}if(b.dataset.openProject){openProject(b.dataset.openProject);return}if(b.dataset.output){state.output=b.dataset.output;render();return}if(b.dataset.toOutput){if(!state.project)openProject('N01');state.output=b.dataset.toOutput;state.drafts[state.output]||={approved:false,note:'',revision:state.revision};navigate('project');return}if(b.dataset.template){useTemplate(b.dataset.template);return}
 if(b.dataset.mode){state.query=$('#query')?.value??state.query;state.mode=b.dataset.mode;render();return}if(b.dataset.query){state.query=b.dataset.query;render();return}if(b.dataset.evidence){showEvidence(b.dataset.evidence);return}if(b.dataset.relation){state.relation=b.dataset.relation;render();return}if(b.dataset.relationLink){state.relation=b.dataset.relationLink;navigate('library');return}if(b.dataset.inventory){inventoryModal(b.dataset.inventory);return}if(b.dataset.slide!==undefined){state.slide=Number(b.dataset.slide);render();return}if(b.dataset.removeFile!==undefined){state.newFiles.splice(Number(b.dataset.removeFile),1);$('#filechips').innerHTML=fileList(state.newFiles);return}
 const a=b.dataset.action;if(a==='close')close();else if(a==='new-project')newProject();else if(a==='switch-project'){state.project=null;navigate('project')}else if(a==='pick-files')$('#files').click();else if(a==='folder')$('#folder').click();else if(a==='expand-data'){state.dataExpanded=!state.dataExpanded;render()}
 else if(a==='upload'){state.newFiles=[];state.fileTarget='existing';modal('프로젝트 자료 추가',`<p style="margin:20px 0">${esc(state.project.name)}</p>${uploadArea()}<div class="modalfoot">${btn('close','취소')}${btn('confirm-upload','프로젝트에 추가','primary')}</div>`)}
 else if(a==='confirm-upload'){if(!state.newFiles.length){$('#fileerror').textContent='추가할 파일을 선택하세요.';return}const count=state.newFiles.length;state.project.files.push(...state.newFiles);state.revision++;state.drafts={};state.project.reviewDrafts=state.drafts;state.project.revision=state.revision;state.history.unshift({name:state.project.name,count,time:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'})});close();render();toast('파일명이 추가되었습니다. 입력 변경으로 기존 초안·승인은 초기화됩니다. 실제 저장 전입니다.')}
 else if(a==='project-files')modal('프로젝트 자료',`<p class="muted" style="margin:18px 0">${esc(state.project.name)} · ${state.project.files.length}개</p><div class="tablewrap"><table><thead><tr><th>문서명</th><th>크기</th></tr></thead><tbody>${state.project.files.map(f=>`<tr><td>${esc(f.name)}</td><td>${Math.ceil(f.size/1024)} KB</td></tr>`).join('')}</tbody></table></div><div class="note">원본 저장·변환·AI 분석 연결 전의 자료 목록입니다.</div>`)
 else if(a==='generate'){state.drafts[state.output]={approved:false,note:'',revision:state.revision};render()}
 else if(a==='edit-draft')modal('초안 검토 의견 수정',`<form id="editdraftform"><label for="draft-note">검토 의견 · 추가 확인 사항</label><textarea id="draft-note" maxlength="2000" placeholder="확인할 조건이나 수정 의견을 남기세요.">${esc(currentDraft().note)}</textarea><p class="fieldhint">저장하면 이전 승인이 해제됩니다. 화면 검토용 의견이며 최종 파일 생성은 연결 전입니다.</p><div class="modalfoot"><button class="btn" type="button" data-action="close">취소</button><button class="btn primary">수정 저장</button></div></form>`)
 else if(a==='approve-draft'){const d=currentDraft();modal(d.approved?'승인 내역 · 화면 예시':'검토 후 승인',d.approved?`<div class="note">제공 N01 예시 초안의 검토 상태입니다. 실제 업무 문서의 승인은 아닙니다.</div><p>검토 의견: ${esc(d.note||'추가 의견 없음')}</p><p class="fieldhint">내용·자료·양식 변경 시 재승인이 필요합니다.</p>`:`<p style="margin:20px 0">원문 근거와 미확정 사항을 확인한 뒤 승인합니다.</p><label class="checkline"><input id="reviewconsent" type="checkbox">제공 자료의 근거와 검토 필요 항목을 확인했습니다.</label><p class="fieldhint">현재 동작은 UI 승인 흐름 예시입니다.</p><div class="modalfoot">${btn('close','돌아가기')}<button class="btn primary" data-action="confirm-approval" id="approve-confirm" disabled>승인하기 · 예시</button></div>`)}
 else if(a==='confirm-approval'&&$('#reviewconsent')?.checked){currentDraft().approved=true;close();render();toast('예시 초안의 승인 상태를 반영했습니다.')}
 else if(a==='confirm-folder'&&$('#folderconsent')?.checked){close();toast('확인한 범위가 기록되었습니다. 실제 저장·지식화는 서비스 연결 후 실행됩니다.')}
});
document.addEventListener('submit',e=>{if(e.target.id==='newprojectform'){e.preventDefault();const name=$('#project-name').value.trim();if(!name){$('#project-name').setCustomValidity('프로젝트 이름을 입력하세요.');$('#project-name').reportValidity();return}const p={id:'UI-'+Date.now(),name,kind:'session',files:[...state.newFiles]};state.projects.unshift(p);close();openProject(p.id)}else if(e.target.id==='queryform'){e.preventDefault();state.query=$('#query').value.trim();render()}else if(e.target.id==='editdraftform'){e.preventDefault();currentDraft().note=$('#draft-note').value.trim();currentDraft().approved=false;close();render();toast('검토 의견을 반영했습니다. 다시 승인해 주세요.')}});
document.addEventListener('input',e=>{if(e.target.id==='project-name')e.target.setCustomValidity('');if(e.target.id==='query')state.query=e.target.value});
document.addEventListener('change',e=>{if(e.target.id==='files')addFiles(e.target.files);else if(e.target.id==='reviewconsent')$('#approve-confirm').disabled=!e.target.checked;else if(e.target.id==='folderconsent')$('#folder-confirm').disabled=!e.target.checked;else if(e.target.id==='provider')state.provider=e.target.value;else if(e.target.id==='folder'){const list=Array.from(e.target.files);modal('기존 폴더 자료 확인',`<p style="margin:20px 0">선택한 범위의 원본 ${list.length}개를 확인합니다.</p><div class="filechips">${list.slice(0,12).map(f=>`<div class="filechip"><span>${esc(f.webkitRelativePath||f.name)}</span></div>`).join('')}</div>${list.length>12?`<p class="small muted">외 ${list.length-12}개</p>`:''}<label class="checkline"><input id="folderconsent" type="checkbox">이 범위의 자료를 지식화할 대상으로 확인했습니다.</label><p class="fieldhint">UI 시안은 목록만 확인합니다. 실제 등록·변환은 연결 후 실행됩니다.</p><div class="modalfoot">${btn('close','닫기')}<button class="btn primary" data-action="confirm-folder" id="folder-confirm" disabled>확인 완료 · 예시</button></div>`);e.target.value=''}});
document.addEventListener('dragover',e=>{const z=e.target.closest('#dropzone');if(z){e.preventDefault();z.classList.add('dragover')}});
document.addEventListener('dragleave',e=>e.target.closest('#dropzone')?.classList.remove('dragover'));
document.addEventListener('drop',e=>{const z=e.target.closest('#dropzone');if(z){e.preventDefault();z.classList.remove('dragover');addFiles(e.dataTransfer.files)}});
document.addEventListener('keydown',e=>{const layer=!$('#modalback').hidden?$('#modalback'):!$('#panelback').hidden?$('#panelback'):null;if(!layer)return;if(e.key==='Escape'){close();return}if(e.key==='Tab'){const list=[...layer.querySelectorAll('button:not(:disabled),a[href],input:not([hidden]),select,textarea')].filter(x=>x.offsetParent!==null);const first=list[0],last=list.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
render();
