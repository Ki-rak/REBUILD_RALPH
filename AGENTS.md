# RE:Build Agent 작업 규칙

## 지시와 작업 단계

- 공식 명칭은 정확히 **RE:Build Agent**. 신규 문서, 화면 제목, 출력 메타데이터에서 축약하지 않는다. 지식조회는 자료 검색과 적용 검토 기능의 명칭이다.
- 현재 사용자의 명시적 지시가 이전 인수인계·목업·설치 도구의 기본 지침보다 우선한다. 보관 중인 원본은 표기를 고치지 않는다.
- 2026-09-21 17:00 KST: /goal 본문 제출 마감. 17:30 KST: 사용자가 /goal 입력, Hands-off 시작. 2026-09-22 12:00 KST: 결과물·세션 로그 제출 마감.
- 사용자가 17:30 전 SPEC·완성 기준·바운더리·오픈소스 하네스 준비를 승인했다. 현재 준비 작업은 제품 구현 완료나 Hands-off 실행으로 집계하지 않는다.
- 제공된 공식 일정 이미지에서 9/22 10:00 KST 봉인 해제를 확인했다 (`docs/event_timetable.png`). 봉인 중 개입 예외·자동 재시작 인정 조건은 미확인이다.
- /goal 본문을 작성하는 것과 활성 goal을 시작하는 것은 다르다. 사용자의 명시적인 실행 지시 전 제품 개발 goal·백그라운드 runner를 시작하지 않는다.
- 범위 확정 이후 통상 구현·수정·검증·복구는 반복 승인 없이 진행한다. 새로운 오픈소스 설치는 사용자가 승인한 목록/범위 내에서만 한다. 하네스 선택·설치는 2026-09-21 대화에서 위임받았다.

## 읽는 순서

1. 현재 사용자 지시, `STATE.json`, `SPEC.md`, `PLAN.md`, `GOAL.md`.
2. `docs/PREPARATION_REPORT.md`, `docs/DECISIONS.md`, `docs/HARNESS.md`, `docs/LOGGING.md`.
3. `REBUILD_Ralphthon_Codex_Full_Context_Handoff_2026-09-20.md`, `REBUILD_Ralphthon_Codex_Handoff.md`, `REBUILD_Hackathon_Step_by_Step_Checklist_KO.md`.
4. `REBUILD_Agent_Initial_Mockup.html`는 DESIGN PREVIEW / 04 화면 참고 자료다. 저장·분석·출력 구현으로 간주하지 않는다.

## 자료 경계

- 작업 루트: `D:\REBUILD_RALPH`. INPUT 루트: `data/REBUILD_INPUT_v1/REBUILD_INPUT_v1`.
- 기존 자료·양식·인수인계·HTML 원본을 삭제, 이동, 덮어쓰지 않는다. `ops/preflight/original-manifest.json`으로 무결성을 확인한다.
- `data/REBUILD_EVALUATOR_v1`은 제품 입력·검색·지식 저장소에서 제외한다. `holdout_cases.json`, `ground_truth_all.json`, `authoring_seed.json`, `source_blocks.jsonl`, `reference_normalized`를 제품 구현용 지식으로 읽거나 복사하지 않는다.
- 평가 README·평가 계약·개발용 평가만 별도 평가 목적에 사용한다. Holdout을 읽게 되면 독립성 상실을 기록한다. 같은 디스크에 있다는 사실은 접근 격리가 아니다.
- `ops/preflight/sample-inspection.json`은 준비용 원문 조사 결과다. 제품은 승인된 INPUT 원본에서 스스로 추출하며 이 파일을 런타임 입력으로 사용하지 않는다.
- 기존 폴더는 대상 목록 확인·사용자 승인 후 지식화한다. 신규 업로드는 원본을 먼저 보존한다. 해시·원래 파일명·문서 개정·발행/승인 상태를 저장한다.

## 제품 불변 조건

- 표·수치·단위·셀 주소·수식·캐시값·페이지·문단·슬라이드 위치를 MD와 JSON sidecar에 보존한다.
- 승인 정정서는 변경한 조항만 대체한다. 미승인 최신본, 이메일, 회의록으로 유효 계약을 덮어쓰지 않는다.
- 과거 관측값과 신규 설계값, 허용량과 설계량, 0과 미확정, 비용과 수입을 구별한다. 근거 없는 수치·점수·적용성은 만들지 않는다.
- 핵심 주장에는 유효 SourceRef가 있어야 한다. 부족·상충·범위 밖은 `REVIEW_REQUIRED`.
- Search Mode와 AI Insight Mode를 구분한다. 모델 연결이 없으면 AI Insight의 미연결 상태와 규칙 기반 비교 범위를 명시한다. 사전 생성 결과를 실시간 AI로 표현하지 않는다.
- 수정 또는 입력 변경 시 기존 승인을 무효화한다. 승인된 내용·입력·근거 버전에 한해서 최종 파일을 생성한다.
- Graph는 실제 프로젝트·문서·지식·근거 관계만 사용하고 클릭하면 원문 근거를 연다.

## 검증·복구·기록

- 작은 입력→추출→비교→근거→승인→실제 XLSX 경로부터 완성한다. 평가 정답/기대값을 고쳐 PASS로 만들지 않는다.
- 준비 검증: `python -X utf8 ops/verify_preparation.py`. 문서 읽기 점검은 번들 Python으로 `ops/inspect_sources.py`를 실행한다.
- 제품 검증 명령은 구현 단계에서 실제 파일과 명령을 만든 뒤 PLAN에 등록한다. 미실시를 PASS로 기록하지 않는다.
- 작업마다 시각·세션 ID·변경·검증·실패/복구·사람 개입을 `ops/activity.jsonl`에 추가하고 STATE를 갱신한다. 기존 로그는 지우지 않는다.
- 실제 세션 로그는 `python -X utf8 ops/export_session.py`로 별도 보존한다. 활성 세션 스냅샷과 종료 로그를 구별한다. API 키·인증정보를 출력하거나 로그에 넣지 않는다.
- 실패 1회: 원인 확인. 2회: 수정 범위 축소/다른 접근. 같은 실패 3회: 변경 보존 후 해당 작업 보류, 독립 작업으로 이동. 전체 초기화 금지.
- 작업 시간이 아니라 실제 근거로 완료 판정한다. 모델 호출 대기·앱 uptime·빈 heartbeat를 자율 개발 시간으로 부풀리지 않는다.
- 내부 운영 제안: 9/22 06:00 대형 변경 중단, 09:00 기능 동결, 11:00 재구조화 중단, 11:40 제출 패키지 준비 완료. 이는 공식 행사 규정과 구별한다.

## 하네스 경계

- Oh My Codex(OMX)를 프로젝트 범위로 사용한다. 사용자 공통 Codex 설정·스킬·인증은 변경하지 않는다.
- Windows에서는 tmux 팀 실행을 필수로 두지 않는다. 기본은 현재 Codex 세션과 OMX CLI 상태·체크포인트. 설치 성공을 연속 실행 성공으로 간주하지 않는다.
- 준비 단계에서는 OMX 계획·상태 저장을 검증한다. 실행 전 fresh goal 상태 확인, 목표 충돌 방지, 실제 검증 근거로 체크포인트한다.
- 하네스의 기본 프롬프트보다 본 프로젝트의 명칭·승인·평가·마감·권한 조건이 우선한다. 권한 우회 옵션, 인증 회전, 사용 한도 우회는 쓰지 않는다.

## 필수 제출 항목

사용자가 제공한 `docs/event_submission_requirements.png`와 `docs/SUBMISSION_REQUIREMENTS.md`를 따른다. 최종 결과/확인법, 작동하는 데모 URL, Google Drive 영상 URL, Public GitHub, 최대 5페이지 발표자료가 필수다. 실제 입력한 /goal 원문과 동일 세션의 입력·결과를 담은 JSONL 1개는 별도 필수 작업 근거다. 추가 JSONL ZIP 1개는 선택. 제품 기능 완성과 행사 제출 준비를 별도로 추적한다. 배포·시연 준비로 제품 기능 완료를 대체하지 않는다. 준비 로그를 주요 개발 로그로 오인하거나 원기록을 합성하지 않는다. 제출물 상태는 `ops/submission-manifest.json`에 기록한다.

## 확정된 배포·API 방향

최신 사용자 지시로 웹 배포 대상은 Vercel이다. 실제 배포와 신규 업로드 전체 흐름 검증 후 데모 URL을 제출한다. LLM 키는 운영진 배포 예정이다. `docs/DEPLOYMENT.md`를 따른다. DB는 Supabase로 확정했다. Supabase Storage/Auth를 기본 구현안으로 사용하며 LLM 공급자는 OpenAI로 확정했고 모델은 설정 후 검증한다. Vercel 함수의 로컬 SQLite/메모리를 영속 저장소로 쓰지 않는다. 실제 키는 서버 Secret 환경에만 두고 값·헤더를 출력하지 않는다. 키 수령 전·설정 후·실제 호출 성공을 구별한다.

## Provider·로그인 추가 범위

`docs/CAPABILITIES.md`의 설치/연결 상태, `docs/PROVIDER_AUTH.md`의 기능 계약을 따른다. Provider 설정·실제 연결 시험 상태와 기본 로그인·세션·로그아웃을 포함한다. 사내 LLM 및 SSO의 실제 연결은 사용자가 추후 결정한다고 했으므로 미설정 상태와 설정 화면을 구현한다. SSO 화면을 실제 사내 인증 성공으로 표현하지 않는다. 사용자별 소유권과 DB/Storage 접근 제어를 검증하고 API 키·서버 secret key를 브라우저나 로그에 노출하지 않는다.

## 신규 업로드 시연 원칙

최종 시연은 DB/지식 인덱스·분석 캐시에 없는 신규 자료를 실제 업로드하는 것으로 시작한다. 기존 지식과의 연결, 원문 근거, 적용성, 검토·승인, 실제 출력이 업로드한 내용에서 생성되어야 한다. P01/N01 고정 결과만으로 완료를 선언하지 않는다. 신규 파일 hash의 사전 부재, 인입·분석 실행, 내용 변화에 따른 결과 변화를 SPEC AC29–30으로 검증한다.

## 제품 완료와 행사 운영의 분리

제품 완료는 기능·데이터 정확성·반복 사용 가능성의 검증으로 판정한다. 배포 URL, 녹화, 발표자료, GitHub 게시와 행사 제출 준비는 별도 운영 작업이며 제품 완료를 대체하거나 제품 완료 판정에 섞지 않는다. ITB XLSX는 첫 구현 단계다. 그 성공이나 한 번의 시연만으로 프로그램 전체가 완성됐다고 선언하지 않는다. 확정된 기능을 구현·검증하고 후속 결과물·그래프의 진행 상태도 구별해 보고한다. 미구현 기능을 완료한 것으로 처리하지 않는다.

API 공급자는 OpenAI로 확정됐다. 로컬 `.env`는 비밀 설정 파일이며 출력·일반 문서 체크포인트·Git·배포 번들에 포함하지 않는다. 키 변수는 OPENAI_API_KEY, 모델은 OPENAI_MODEL, URL은 OPENAI_BASE_URL이다. 신규 문서 스냅샷은 `.env.example`만 포함한다.

UI·출력의 최신 계약은 docs/UX_DECISIONS.md와 SPEC AC31–38이다. 데이터 등록 승인과 결과물 최종 승인을 구분하고, 녹색 최신 상태/리스크 강도는 실제 근거로만 표시한다. 수정 화면 설계안은 docs/ui-review/index.html이며 기존 목업보다 최신 참고자료지만 제품 분석/저장 구현으로 간주하지 않는다.

수정 화면안의 고정 예시 데이터·복사된 source 첨부는 UI 검토용이다. 제품 검색·추론의 런타임 입력이나 신규 업로드 분석 결과로 사용하지 않는다. 실제 기능은 승인된 원본 및 사용자가 업로드한 bytes에서 생성한다.


## 최신 사용자 확정 (2026-09-21, /goal v1.0)

GOAL.md와 ops/GOAL_INPUT.txt의 자체 완결형 원문이 현재 실행 계약이다. 필요한 프로젝트 범위 오픈소스·스킬·플러그인 설치는 이번 지시로 승인됐으며 개별 재승인 없이 설치·버전 기록·구동 검증한다. 각 의미 있는 작업 단위는 검증 후 로컬 Git 커밋하고 실패 체크포인트는 구분한다. Supabase 실제 DB/Storage/Auth/RLS 연결 검증은 필수다. 배포·공개 게시·데모/영상/발표자료·행사 제출은 추후 사용자 요청까지 실행 범위에서 제외한다. 제품 기능 완료까지 OMX 구현→실행/검토→실패 수정→재검증→기록/커밋/체크포인트 루프를 계속한다. 이 문단과 현재 사용자 지시가 이전 설치 승인·배포 운영 문구보다 우선한다.


## UI 수정 06 — 최신 사용자 지시

현재 UI 계약은 docs/UX_DECISIONS.md와 docs/ui-review/index.html이다. 지식조회로 명칭을 변경하고, 새 프로젝트 이름 아래 파일 업로드를 제공한다. ITB 분석표·심의장표·Risk Register의 초안·근거·검토·승인 흐름을 유지한다. 지식 자료실은 목록/관계 보기 전환 없이 기본 지식 연결 노드와 과거↔신규 비교 설명을 함께 보여준다. 데이터 관리에는 과거 프로젝트/문서 보유량과 새로 유입된 문서를 표시하고 실패/처리중 KPI와 등록 대상 섹션은 제거한다. 폴더 자료 확인·승인은 별도 작업에서 유지한다. 실제 양식 폴더는 data/REBUILD_INPUT_v1/REBUILD_INPUT_v1/03_OUTPUT_TEMPLATES이며 제공 7종을 표시한다.

이번 요청은 UI 수정이다. GOAL.md·ops/GOAL_INPUT.txt·OMX 목표는 사용자 요청에 따라 수정하지 않았으며 후속 /goal 개정 시 최신 UI 계약을 반영해야 한다. 기존 goal의 명칭·화면 지시보다 이번 UI 지시가 우선한다. 이 시안은 백엔드·Supabase·LLM·최종 파일 생성 완료의 증거가 아니다.


## /goal v1.1 — AI 인증 분리 (최신)
GOAL.md와 ops/GOAL_INPUT.txt v1.1은 작성된 실행 계약이며 실제 /goal 입력과 다르다. 사용자는 이번에 server/ai 인증 경로의 실제 구현을 별도로 지시했다. 전체 제품 goal은 명시적 입력 전 시작하지 않는다. AI는 로컬 개발·로컬 데모에서 공식 Codex SDK/CLI ChatGPT OAuth, 배포용 서버에서 OpenAI API key로 분리한다. 로컬 API key 또는 배포 OAuth의 묵시적 fallback은 금지한다. 전용 프로젝트 인증 디렉터리와 최소 권한 실행 환경을 사용하고 OAuth 토큰을 직접 추출하지 않는다. 개인 구독을 공개 프록시로 제공하지 않는다. 환경·로그인 상태·모델 호출 성공·전체 제품 완성을 구별한다. AC39–42와 docs/PROVIDER_AUTH.md를 따른다. 기존 모든 환경 API-key 규정은 이 문단으로 대체한다. 기존 UI 수정 06의 goal 개정 보류는 이번 개정 요청으로 해소되었다.
필요 도구는 프로젝트 범위로 검토·설치·사용하고 검증된 작업 단위 및 세션 전환마다 로컬 Git 커밋한다. 독립 구현/리뷰는 설치된 개발 스킬과 OMX 최종 strict gate에 따라 필요한 경우 수행한다. 배포·공개 푸시·행사 제출은 여전히 별도 요청까지 제외한다.

## 2026-09-22 배포 및 UI 복원 승인
최신 사용자 지시로 남은 수정, GitHub main 공개 푸시, PLAI Developer League 2026 팀의 g-19 Vercel production 배포가 승인되었다. docs/ui-review/index.html의 v6 시각·기능 복원과 프로젝트 이름 변경·삭제 관리도 포함한다. 7개 서버 환경변수의 g-19 production/preview 등록은 별도 명시 승인했다. 기존 공개/배포 보류 문구보다 이 지시가 우선한다. 원본 보존·비밀 제외·미검증 미완료 원칙은 유지한다.
